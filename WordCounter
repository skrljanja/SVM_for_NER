from CountUtils import countValue
import sys 

filepath = sys.argv[1]

def countWords(line):
    seperator = '(\s)+'
    valueName = "words"
    countValue (line, seperator, valueName)


with open(filepath) as file:
         for line in file:
            countWords(line)
